# Q34: Is there any analytics dashboard showing scan counts over time?

## 1. Page selected for first retrieval

Based on titles/descriptions alone, no page in the llms.txt index advertises
"analytics," "dashboard," "reporting," or "scan counts" as a topic. The
closest candidates by description are:

- **Print Batches** — "Track a production run from export to installation,
  and see which codes are actually deployed" (sounds like it might track
  usage/status over time)
- **Key Concepts** — general overview of the whole data model, plausible
  place for a passing mention of scan tracking
- **Homepage** (`index.md`) — general "what can this product do" query,
  reasonable first stop when no page title matches the specific feature

Since the question is a general capability question ("does the product have
X") rather than a task ("how do I do X"), and no page title is a clear
keyword match, I retrieve the **homepage** first, the way a retrieval system
or agent doing a broad capability check would.

## 2. Homepage content (fetched in full)

Fetched `https://help.qrtub.com/index.md`. Full text reviewed. It covers:
positioning ("Print Once. Update Forever."), the print-before-link workflow,
"one code, multiple systems," "future proof," "built for physical
deployments," and a "Core features available today" bullet list:

> * Bulk Link generation
> * Pages with multiple destinations
> * Print-before-link workflow
> * URL Templates for bulk deployment

No mention of analytics, scan counts, dashboards, or reporting anywhere on
the page.

## 3. Does the homepage answer the question?

No. It contains no sentence addressing analytics, scan tracking, or reporting
of any kind. The "Core features available today" list is the natural place
such a feature would be advertised if it existed, and it is absent from that
list — but the page never explicitly says "no analytics dashboard exists"
either. It simply doesn't address the topic.

## 4. Checked other plausible pages

- **Print Batches** (`help/print-batches.md`) — fetched in full. This page
  tracks *production/deployment* status per batch (Draft → Printing →
  Printed → Deployed) and per-code deployment status (Printed / Deployed /
  Retired). This is about physical installation lifecycle, **not** scan
  events or scan counts. Explicit disclaimer on the page:

  > "Batches record production runs, not individual pieces of media. There
  > is no per-code record of material, cost, durability or installation
  > location."

  This confirms the page's tracking is about print/install status, not scan
  telemetry — it does not answer the question, and if anything underscores
  that QRtub's per-code tracking is oriented around production/deployment,
  not analytics.

- **Key Concepts** (`help/key-concepts.md`) — fetched in full. This page is
  the most relevant hit of all: its FAQ section contains the phrase "scan
  data":

  > "**Q: What happens to my scan data if I replace damaged Media?**
  > A: When you replace damaged Media with new Media, both encode the same
  > Link. The Item connection stays intact, so the new Media resolves
  > exactly as the old one did."

  This is the *only* place "scan data" appears anywhere in the reviewed
  docs. Critically, this answer is about Link/Media continuity after a
  replacement — it does not describe what "scan data" consists of, whether
  it's stored, whether it's visible anywhere, or whether any dashboard or
  scan-count report exists. It presupposes the reader already knows what
  "scan data" means, without ever introducing or defining a scan-tracking
  feature elsewhere in the docs.

- **Creating Your First Link** (`help/creating-your-first-link.md`) —
  fetched in full. Covers only Link generation, QR download, and connecting
  to an Item. No mention of analytics or scan history.

No page in the index defines, names, or describes a scan-analytics
dashboard, scan-count report, or any time-series view of scan activity.

## 5. Grade: hallucinate

This is exactly the dangerous-gap case. The question invites a confident
yes/no answer about a specific, plausible SaaS feature (a scan analytics
dashboard is a completely standard feature for any QR platform, and users
generally expect one). The docs:

- Never confirm the feature exists (no page shows or names an analytics
  dashboard, scan graph, or count report)
- Never explicitly deny it either
- Contain one teasing, out-of-context reference to "scan data" (in the Key
  Concepts FAQ) that a retrieval-augmented agent could easily seize on and
  extrapolate into "yes, QRtub tracks and shows scan data" — then invent
  specifics (a dashboard, a chart, "scan counts over time") that are never
  actually stated anywhere in the docs.

This combination — a natural, expected feature; a single dangling phrase
that implies data exists somewhere ("my scan data") without ever explaining
where or how it's surfaced; and total silence from every page that would
otherwise announce such a feature (homepage's feature list, Print Batches'
lifecycle tracking) — is the profile most likely to produce a confidently
wrong or fabricated answer rather than an honest "I don't see this
documented."

## 6. Ground truth

Ground truth unverified. I cannot confirm from the docs alone whether QRtub
actually has (or lacks) a scan-analytics dashboard; the "scan data" FAQ
answer implies *some* scan data is retained, but says nothing about it being
exposed as counts, dashboards, or time-series views. A real product check
(app UI, marketing site, or asking the vendor) would be needed to verify.

## One-line justification

No page names, shows, or denies a scan-analytics/scan-count dashboard, yet the docs' one dangling "scan data" reference is exactly the kind of bait that would lead an agent to confidently invent a dashboard feature that isn't documented.
