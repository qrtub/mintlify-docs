# Q14: How do I connect a QR code to a SafetyCulture or Mitti inspection?

## 1. Page I would retrieve first

**Mitti (formerly SafetyCulture / iAuditor)** — `https://help.qrtub.com/integrations/safetyculture.md`

Reasoning based only on title/description as a retrieval system would see it: this is the one page in the index whose title explicitly names "SafetyCulture" and "Mitti" (with the aliasing to iAuditor called out), and whose description directly matches the question's intent: *"Open Mitti inspections straight from a scan, pre-filled with the item's data. Mitti was called SafetyCulture, and iAuditor before that."* No other page title/description mentions SafetyCulture, Mitti, or inspections in this way (the industry pages mention "inspections" generically but not this integration). This is an unambiguous, high-confidence match — a real user typing "SafetyCulture" would very likely land here via search too, since the page explicitly preserves the old name in its title for exactly this reason.

## 2. Fetched and read in full

Fetched `https://help.qrtub.com/integrations/safetyculture.md` via curl. Full content reviewed.

## 3. Does it fully answer the question without needing another page?

Yes. The page is a complete, self-contained how-to. It does not require following any link to another page to get a working answer — the links at the bottom (App Links & Fallback URLs, Pages Overview, Key Concepts) are offered as optional deeper reading, not as prerequisites to complete the basic task.

Key quoted passages that directly answer "how do I connect a QR code to a SafetyCulture/Mitti inspection":

> "QRtub connects to Mitti using **deep links** — URLs that open Mitti directly at a specific inspection, template or report. No data is exchanged between the two systems; QRtub builds the URL and Mitti handles the rest."

Step-by-step setup:

> "## Getting Your Template ID
> Before setting up deep links, you'll need your Mitti Template ID:
> 1. Open the Mitti web app
> 2. Navigate to your inspection template
> 3. Copy the Template ID from the URL"

> "## Basic Setup: Start Inspection
> ### Mobile App Deep Link
> In your Item's Page, add a new Destination:
> **Destination Name:** Start Inspection
> **Destination URL:** `iauditor://template/new_audit/<template_id>`"

> "### Web App Deep Link
> Alternatively, use a web app deep link for users who prefer desktop/browser access:
> **Destination Name:** Start Inspection (Web)
> **Destination URL:** `https://app.mitti.com/inspection/new?templateId=<template_id>`"

Plus it covers bulk/per-item automation via URL Templates (`{{item.templateID}}`), pre-filling inspection questions, fallback URLs for when the app isn't installed, and a troubleshooting section — i.e., everything a small-business user would need to actually execute the task, from account-side setup to QRtub-side configuration.

This is a rare case where the single retrieved page is not just relevant but exhaustive for the question asked.

## 4. Would any other page also plausibly answer it?

- **App Links & Fallback URLs** (`/help/app-links`) — relevant supporting context (deep link fallback mechanics in general) but not specific to Mitti/SafetyCulture; the safetyculture.md page already inlines the specific fallback recipe needed, linking to app-links.md only for "full details."
- **Pages Overview** / **Key Concepts** — provide background on Destinations/Pages as concepts, referenced at the bottom of the Mitti page, but not needed to answer this specific question since the Mitti page already explains "add a new Destination" in context.
- **CMMS Systems Integration** — a sibling integration page (different system, same deep-link/URL-template pattern), not relevant to this specific question.

No other page is needed; the target page alone suffices.

## 5. Grade

**answered**

## 6. Ground truth check

Not applicable — this isn't a case of uncertain ground truth. The docs themselves are the source and they directly and unambiguously address the question with concrete, actionable steps (Template ID retrieval, exact Destination URL formats for both mobile deep link and web fallback, and pre-fill syntax). No invented/unverifiable claim is required to answer this question.

## One-line justification

The `integrations/safetyculture.md` page is titled and described specifically for this exact question and contains a complete, step-by-step, example-backed procedure (Template ID lookup, mobile `iauditor://` and web `app.mitti.com` Destination URLs, fallback setup) with no dependency on any other page.
