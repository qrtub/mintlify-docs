# Q22: What happens if someone scans a code and doesn't have the required app installed?

## 1. Page selected from index (title/description only)

**App Links & Fallback URLs** (https://help.qrtub.com/help/app-links.md)
— "Use deep links to open mobile apps directly, with automatic fallback when the app isn't installed"

This is the obvious first choice: the description contains almost the literal question ("with automatic fallback when the app isn't installed"). No other page description mentions apps, deep links, or "installed" at all. Runner-up candidates that a retrieval system might also surface: **Device Detection & Routing** (routes by OS/browser, not by app-installed state) and **Mitti (SafetyCulture)** (a specific app integration, narrower than the general question). Neither description addresses "app not installed" as directly as App Links & Fallback URLs does.

## 2. Fetched and read in full

Fetched `https://help.qrtub.com/help/app-links.md` in full (curl). Full content reviewed above the analysis.

## 3. Does it fully answer the question without following another link?

Yes. The page has an entire section titled "The Problem: App Not Installed" and "How QRtub Handles App Links" that directly answer this.

Exact quoted answer:

> "QRtub Destinations support app deep links — special URLs that open a mobile app directly to a specific screen. When someone scans and the app isn't installed, QRtub automatically falls back to a URL you specify, keeping the experience useful rather than dead."

And the mechanism, in detail:

> "QRtub detects when a Destination URL is an app link (any non-`https://` scheme). At scan time:
> 1. QRtub attempts to open the app link
> 2. A 2.5-second timer starts
> 3. **If the app opens** — the timer is cancelled, the user is in their app
> 4. **If the page is still visible after 2.5 seconds** — the app wasn't installed, QRtub navigates to your Fallback URL
>
> If no Fallback URL is set, QRtub shows a message instead — either your custom Fallback Message, or a default 'App not available' notice."

This gives a complete, specific answer: what happens (fallback URL navigation after a 2.5s timeout), what happens if no fallback URL is configured (custom Fallback Message, or default "App not available" notice), and how to configure it (Destination/Link/Item level precedence). No need to follow a link to another page — this page is self-contained for the question asked.

## 4. Other pages that plausibly cover this

- **Device Detection & Routing** — complementary but narrower; explicitly cross-referenced from the App Links page itself as relevant for routing iOS/Android to different app stores or handling browsers that block app-link schemes (e.g., Chrome on iOS blocking `iauditor://`). Doesn't independently answer "app not installed," but supports edge cases.
- **Mitti (SafetyCulture) integration page** — likely reiterates the same fallback pattern for one specific app, but is a special case of what App Links & Fallback URLs already documents generally.
- **Key Concepts** — describes Link modes generally but not app-link/fallback behavior specifically (based on its description; not fetched, since the App Links page already gave a complete answer).

No other page is needed; App Links & Fallback URLs is sufficient and authoritative on its own.

## 5. Grade

**answered**

## 6. Ground truth

Not independently verified against the live product (I only read the docs page, not the running QRtub app), but the docs page is detailed, internally consistent, and specific (exact timer value, precedence rules, default message text) in a way that reads as accurate product documentation rather than vague marketing copy. Treating it as the ground truth for this retrieval exercise is reasonable; flagging that I have not cross-checked it against the actual app behavior.

## One-line justification

The retrieved page's description already named the exact scenario in the question, and its body gives a precise, self-contained mechanism (deep-link attempt → 2.5s timer → Fallback URL or Fallback Message/default notice) with no need to consult any other page.
